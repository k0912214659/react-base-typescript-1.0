import React, { memo } from 'react';

function Overview(): React.ReactElement {
  /* Global & Local State */
  /* Functions */
  /* Main */
  return (
    <div>
      Overview
    </div>
  );
}

export default memo(Overview);
