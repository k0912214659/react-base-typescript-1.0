import React, { memo } from 'react';

function SignIn(): React.ReactElement {
  /* Global & Local State */
  /* Functions */
  /* Main */
  return (
    <div>
      SignIn
    </div>
  );
}

export default memo(SignIn);
