import React from 'react';
import './index.css';
import { AppProps } from './types';

const App: React.FC<AppProps> = (): React.ReactElement => (
  <div className="App">
    First React APP
  </div>
);

export default App;
